import {assert} from "chai";
import {UnitRole} from "../../src/constants";


describe("constants", () => {
  before(() => {
    // runs before all test in this block
  });

  beforeEach(() => {

  });

  it("it will run", () => {
    assert.isTrue(UnitRole.NULL === "null")
  });


});
